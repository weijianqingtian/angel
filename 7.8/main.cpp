#include <iostream>
#include <iomanip>
#include <array>
using namespace std;

int main()
{
    char alphabet[6];
   {
       float grades[4];
    cout << "grades[" << 4 << "]=";
    cin >> grades[4];
   }
   array< int, 5>values = { 8 };
   for ( sum = 0, i = 0; i < 100; i++ )
    sum += temperatures[ i ];
    for ( double i = 0; i < 11; ++i )
        b[ i ] = a[ i ];
        array<double, 11>a;
        array<double, 34>b;
        for(unsigned i=0; i<=10; i++)
        {
            b[ i ] = a[ i ];
            cout << "b[" << i << "]" << "is" << b[ i ] << "\n";
        }
        int h,Max = w[ 99 ],Min = w[ 99 ];
        for ( i = 0; i<99; i++ )
        {
            if(Max<w[ i ])
            {
                Max = w[ i ];
                h=i;
            }
            if(Min>w[ i ])
            {
                Min = w[ i ];
                h=i;
            }
        }
        cout << "Max:" << "a[" << h << "]=" << a[ h ] << endl;
        cout << "Min:" << "a[" << h << "]=" << a[ h ] << endl;




}
