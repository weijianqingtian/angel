#include <iostream>

using namespace std;

int main()
{
    a) int *numberPtr;
    cout << number << endl;
    b) 两个指针变量的数据类型不同，编译时会出错。
    double *realPtr;
    long *integerPtr;
    *integer = *number;
    c) int *xPtr, *yPtr;
    x = y;
    d) char s[] = "this is a character array";
       for ( ; *s != '\0'; ++s )
            cout << s << ' ';
    e) short *numPtr, *result;
       void *genericPtr;
       *genericPtr = *numPtr;
       result

}
