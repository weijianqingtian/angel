#include <iostream>

using namespace std;

int main()
{
    a) array< int, 10 > counts = {};
    b) for (i=0; i<15; i++)
    bonus[i]++;
    c) for (i=0; i<12; i++)
    scanf("%f",&monthlyTemperatures[i]);
    d) array<int, 5>bestScores={};
    for(int i=0, i<5, i++)
    {
        cin >> bestScores[i];
        cout << bestScores[i];
    }
}
